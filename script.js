const ordenados = Lista-numros-ordenados;

const conteudoJson = '{';
    conteudoJson += '"numero": "'+ conteudoArray
conteudoJson += '}';


let arquivo = new Arquivo [conteudoJson],{type:"text/plain;charset=utf-8"}
saveAs(blo, ordenados+".json")